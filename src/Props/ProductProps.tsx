export type ProductProps = {
    product_name: string
    quantity: number
    image_url: string
    description: string
    branch_name: string
    location: string
    latitude: string
    longitude: string
}

// type Props = { ProductProps:ProductProps }

